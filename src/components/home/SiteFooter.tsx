export function SiteFooter() {
  return (
    <footer
      className="relative mt-auto w-full max-w-[var(--footer-max-width)] shrink-0 overflow-hidden"
      style={{
        height: "var(--footer-height)",
        background: "var(--footer-gradient-placeholder)",
      }}
    />
  );
}
